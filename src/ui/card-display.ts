// Contract Crown Card Display
// Card display component

export class CardDisplay {
  public renderCard(card: any): void {
    // Render single card
  }

  public renderHand(cards: any[]): void {
    // Render player hand
  }
}
