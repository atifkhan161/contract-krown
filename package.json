{
  "name": "contract-crown-game",
  "version": "0.1.0",
  "description": "Mobile-first PWA for Contract Crown trick-taking card game",
  "type": "module",
  "main": "src/server/index.ts",
  "scripts": {
    "dev": "bun run build:client && bun run --watch src/server/serve.ts",
    "build": "bun run build:server && bun run build:client",
    "build:server": "bun build src/server/serve.ts --outdir dist/server --target bun",
    "build:client": "bun run build:js && bun run build:css && bun run build:copy",
    "build:js": "bun build src/ui/app.ts --outdir dist/client --target browser",
    "build:css": "tailwindcss -i src/ui/styles/main.css -o dist/client/styles.css --minify",
    "build:copy": "cp public/index.html public/manifest.json dist/client/",
    "start": "bun run dist/server/index.js",
    "test": "bun test",
    "test:unit": "bun test --run",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "lint": "tsc --noEmit && eslint src tests --ext .ts,.tsx"
  },
  "dependencies": {
    "elysia": "^1.1.16",
    "colyseus": "^0.15.23",
    "lokijs": "^1.5.12",
    "page": "^1.8.7",
    "tailwindcss": "^3.4.17",
    "daisyui": "^4.12.14"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "@types/lokijs": "^1.5.8",
    "@types/page": "^1.8.3",
    "autoprefixer": "^10.4.27",
    "daisyui": "^4.12.14",
    "eslint": "^8.57.1",
    "eslint-config-standard-with-typescript": "^43.0.1",
    "fast-check": "^3.18.0",
    "postcss": "^8.5.8",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "peerDependencies": {
    "typescript": "^5.7.2"
  }
}
